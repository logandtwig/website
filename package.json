{
  "name": "landingpage-lnt",
  "version": "1.0.0",
  "description": "Landingpage for the lnt Business",
  "style": "css/lnt.css",
  "sass": "scss/lnt.scss",
  "scripts": {
    "start": "npm run prepare && npm run sass",
    "prepare": "rm -rf webfonts js && mkdir webfonts js && cp node_modules/@fortawesome/fontawesome-free/webfonts/* webfonts && cp node_modules/bootstrap/dist/js/bootstrap.bundle.min.js js/bootstrap.bundle.min.js && cp node_modules/jquery/dist/jquery.slim.min.js js/jquery.slim.min.js",
    "sass": "node-sass --output-style compressed scss/lnt.scss > css/lnt.css",
    "watch": "npm run prepare && npm run sass -- -w",
    "build": "npm run start && rm -rf dist && mkdir dist && cp -a index.html css js img webfonts dist/"
  },
  "author": "Pär Nilsson",
  "license": "MIT",
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.11.2",
    "autoprefixer": "^9.7.3",
    "bootstrap": "^4.4.1",
    "jquery": "^3.4.1",
    "node-sass": "^4.13.0",
    "popper.js": "^1.16.0",
    "postcss-cli": "^6.1.3",
    "stylelint": "^10.1.0",
    "stylelint-config-recommended-scss": "^3.3.0",
    "stylelint-config-standard": "^18.3.0",
    "stylelint-order": "^3.1.1",
    "stylelint-scss": "^3.13.0"
  }
}
